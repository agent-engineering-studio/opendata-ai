/** @type {import('next').NextConfig} */
// Plain Next.js app (NOT static export — it uses server-side API routes to
// proxy the A2A JSON-RPC call, so there are no CORS constraints).
const nextConfig = {};

export default nextConfig;
